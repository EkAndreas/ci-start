# virtuell maskin, Ubuntu 12-10

# logga in som root!
sudo su

# ta reda på adresserna till din virtuella server!
ifconfig

# installera ssh
apt-get install -y openssh-server

# logga in med terminalklient istället för virtuell player/win

# modifiera hostfilen
nano /etc/hosts
#exempelvis: 10.211.55.2	ci.local

#uppdatera servern
wget -q -O - http://pkg.jenkins-ci.org/debian/jenkins-ci.org.key | sudo apt-key add -
sh -c 'echo deb http://pkg.jenkins-ci.org/debian binary/ > /etc/apt/sources.list.d/jenkins.list'
apt-get update && apt-get upgrade -y

#installera alla tjänster
apt-get install -y default-jre git xvfb firefox jenkins

#länka firefox (bug i selenium)
ln -s /usr/lib/firefox/firefox /usr/bin/firefox-bin

#installera selenium standalone
wget http://selenium.googlecode.com/files/selenium-server-standalone-2.32.0.jar
mkdir -p /var/lib/selenium
mv selenium-server-standalone-2.32.0.jar /var/lib/selenium/selenium.jar

